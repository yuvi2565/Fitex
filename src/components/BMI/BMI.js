import React from 'react'

function BMI(props) {
//   const CurrentBmi = props.userData.CurrentBmi;
//   const currentBmiStage = props.userData.currentBmiStage;
//   const heightFeet = props.userData.heightFeet;
//   const heightInches = props.userData.heightInches;
//   const currentWeight = props.userData.currentWeight;


//   const heightInMeters = (heightFeet * 12+ heightInches)*0.0254;
//   const bmi = currentWeight / (heightInMeters * heightInMeters);


//   let bmiStage = '';
//   if (bmi < 18.5) bmiStage = 'Underweight';
//   else if (bmi >= 18.5 && bmi <= 24.9) bmiStage = 'Normal';
//   else if (bmi >= 25 && bmi <= 29.9) bmiStage = 'Pre-obesity';
//   else if (bmi >= 30 && bmi <= 34.9) bmiStage = 'Obesity Class 1';
//   else if (bmi >= 35 && bmi <= 39.9) bmiStage = 'Obesity Class 2';
//   else if (bmi >= 40) bmiStage = 'Obesity Class 3';

// console.log(bmi);
// console.log(bmiStage);
  // props.updateUserData({CurrentBmi:bmi,currentBmiStage:bmiStage });
  return (
    <div>BMI</div>
  )
}

export default BMI